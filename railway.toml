# Railway Deployment für smartlaw

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run start:prod"
healthcheckPath = "/api/auth/me"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
